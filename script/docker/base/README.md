```shell
cd script/docker/base
# 运行
docker compose -p base up -d
# 停止
docker compose -p base down
```

`Nacos`访问地址: http://127.0.0.1:8848/nacos 用户名: `nacos` 密码: `nacos`
`xxl-job-admin`访问地址: http://127.0.0.1:9090/xxl-job-admin 用户名: `admin` 密码: `123456`